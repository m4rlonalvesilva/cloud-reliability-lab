apiVersion: v1
kind: Pod
metadata:
  name: dns-test
  namespace: lab-ns-vendas
  labels:
    app: dns-test
spec:
  containers:
    - name: shell
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
